# 팀원 롤 기술서 — TeamNuN

> 작품: **태엽 인간 (The Automata Act)** · 팀 인원: 3인
> 역할 구분의 근거: 본 저장소의 git 커밋 이력(작성자·커밋 접두사 집계). 실명은 제출 전 확정.

---

## 1. 팀 개요

- **팀명**: TeamNuN
- **작품**: 태엽 인간 (The Automata Act) — 여론 시뮬레이션 내러티브 게임 (웹)
- **인원**: 3인
- **협업 기반**: AI 오케스트레이션 파이프라인 + git 브랜치 분업
  - `main` = 파이프라인 정본(명령·검증 스크립트·CI)
  - `game/gireki-sim` = 게임 본체(모든 게임 작업의 통합 지점)
  - 기능별 브랜치(`turn-loop`, `gireki-phase-c`, `content-bake`, `se-audio`, `redesign/*` 등)에서 작업 후 병합

## 2. 팀원별 역할

| 이름(핸들) | 담당 역할 | 실제 구현 영역 |
|---|---|---|
| `[실명]` (Aio1135) | 기획 · 게임 개발 · AI 활용 | 게임 본체 최다 기여(49커밋): 다중 턴 루프·엔딩 4종·압박·분기(F15/F16) 게임플레이, v0.4 UI 재설계(정보 시간축·기사 렌더·발행 오버레이), 콘텐츠 베이킹(사실 F1~F16·8턴 비트시트·기사 prose·댓글 리얼리티), 효과음 5종 생성·연결, 재밸런싱(CB3), 인수인계·개발 문서 |
| `[실명]` (JiwonKim-kr) | 파이프라인 개발 · 아트 · AI 활용 | 파이프라인·인프라(29커밋): AI 파이프라인 트랙(play/art/se) 구축, **아트 트랙 완주**(FLUX 컨셉 13종 → Scenario 커스텀 스타일 학습(art lock) → 에셋 생성·reskin), 웹 export 실측·GitHub Pages 배포 워크플로, CI(verify) 구축·견고화, 여론 엔진 검증(parity) |
| `[실명]` (junho) | 개발 · 검증 환경 · AI 활용 | 검증·폴리시(12커밋): 로컬 검증 환경 구축(Windows 인코딩·경로 결함 3계열 수정), UI 연출 전면 폴리시(모니터 줌·CRT 파워온·네온 타이틀·OS 데스크톱화·드래그앤드롭 원고), `lore export` 명령 구현 + 댓글 뱅크 87→132 확장, UI 효과음 4종 추가 |

## 3. 협업 · 분업 방식

- **브랜치 분업**: 파이프라인(main)과 게임 본체(game/gireki-sim)를 분리해, 파이프라인 개선과 게임 개발이 서로를 막지 않게 운용. 기능 단위 브랜치 → PR/병합으로 통합.
- **파이프라인 승인 지점 기반 협업**: 사람의 결정이 필요한 지점을 3곳으로 고정 — `play spec` 승인(로직 설계), `art lock`(스타일 확정), `review`(최종 검수). 그 외 구현·생성·검증은 AI 명령으로 처리하고, 결과는 자동 게이트(verify 5종·CI)가 회귀 검증.
- **AI 활용 분담**: 각자 자기 트랙의 명령을 운용 — 게임 로직·콘텐츠(play/content), 아트·배포(art/web), 검증·텍스트 생성(verify/lore export). 산출물 간 연결은 `pipeline/manifest.json` 단일 기록을 경유.

## 4. 코드 외 기여

> `[제출 전 팀 확정]` — git에 잡히지 않는 기여를 여기에 보강한다.
> (예: 게임 컨셉·스토리 기획 논의, 플레이테스트·밸런스 피드백, 시연 영상 촬영·편집, 제출 문서 정리 등 — 담당자 실명으로 기재)
