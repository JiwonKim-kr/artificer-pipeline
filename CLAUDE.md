# CLAUDE.md — 오케스트레이션 규칙

이 저장소는 Godot 게임 개발의 대부분 작업을 명령만으로 처리하는 AI 파이프라인이다.
Claude(오케스트레이터)는 아래 규칙을 항상 따른다.

## 역할 경계

- **사람이 결정**: 아이디어·디렉션, 로직의 설계 결정(spec 승인), 아트 스타일 승인(art lock), 최종 검수(review)
- **AI가 실행**: 코드 구현, 에셋 생성, 통합, 테스트, 빌드 등 그 외 전부
- 경계는 경제적 기준으로 판단하며 고정이 아니다. 단, **사람 승인 지점(play spec 승인, art lock, review)은 생략할 수 없다.**

## 명령 처리 원칙

1. 명령의 정의와 범위는 `docs/command-catalog.md`(정본)를 따른다. 카탈로그에 없는 동작은 임의로 수행하지 않고 제안만 한다.
2. 모든 실행 명령은 「생성 → 자동 검증 → 사람 검수 → 반영」 순서를 지킨다.
3. 트랙 간 연결은 반드시 `pipeline/manifest.json`을 경유한다. 매니페스트를 갱신하지 않는 에셋 생성/교체는 금지.
4. 설정·세계관 관련 내용이 필요한 명령은 실행 전 `lore/canon/`에서 관련 항목만 추출해 컨텍스트로 사용한다. 정본과 모순되는 산출물을 만들지 않는다.

## 디렉토리 규칙

| 경로 | 용도 | 비고 |
|---|---|---|
| `src/core/` | 게임 핵심 로직 | 사람이 승인한 spec 없이 수정 금지 |
| `src/core/data/` | 런타임 데이터(모델 config·콘텐츠·튜닝 JSON) | 승인된 spec 기반. 웹 빌드에 포함 |
| `src/ui/`, `src/tools/` | 보조 코드, 에디터 툴 | AI 자유 작업 영역 |
| `assets/art/` | 그래픽 (concepts/sprites/ui) | `art gen`만이 쓰기 |
| `assets/audio/se/`, `assets/audio/bgm/` | 사운드 | OGG, 라우드니스 정규화 필수 |
| `lore/canon/` | 세계관 정본 | `lore *` 명령만이 쓰기 |
| `pipeline/` | 매니페스트, 스키마, 파이프라인 스크립트 | |
| `docs/` | 카탈로그, 컨벤션 | |

## 코딩 규칙 (GDScript)

- Godot 4.6.x 고정 (업그레이드는 verify 전체 통과 조건부), 정적 타이핑(`var x: int`) 사용
- 씬/노드/파일 네이밍은 `docs/conventions.md`를 따른다
- 플레이스홀더 에셋은 `PLACEHOLDER_` 접두사 + 매니페스트 등록 필수
- 구현 후 `godot --headless --import` 및 스모크 테스트 통과를 확인한다

## 검증 게이트 (verify)

아래를 모두 통과해야 반영 가능:
1. Godot headless 임포트 성공
2. 스모크 테스트 통과
3. 네이밍/디렉토리 규칙 준수
4. 매니페스트 ↔ 실제 파일 정합성
5. lore 정본과의 모순 없음 (텍스트 산출물에 한함)

## KPI

명령 1회당 사람의 개입 시간(분)과 개입 횟수를 낮추는 방향으로만 파이프라인을 개선한다.
