# lore/ — 세계관 정본 (canon)

- `canon/` 하위 문서가 프로젝트의 유일한 설정 정본이다.
- 쓰기는 `lore *` 명령을 통해서만 수행한다.
- 다른 트랙의 명령은 실행 시 관련 항목만 추출해 컨텍스트로 주입받는다.
- 권장 구조: `canon/world.md`(세계), `canon/factions.md`(세력), `canon/characters/`(인물), `canon/glossary.md`(용어)
